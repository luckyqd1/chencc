#这是一个基本的工作流程，可以帮助您开始使用Actions

姓名:

#控制工作流何时运行
关于：
#在推或拉请求事件上触发工作流，但仅针对"主"分支
推
分支机构：【主要】
合并请求：
分支机构：【主要】

#允许您从“操作”选项卡手动运行此工作流
工作流_分派：

#一个工作流运行由一个或多个作业组成，这些作业可以顺序运行，也可以并行运行
工作：
#此工作流包含一个名为“构建”的单一作业
构建：
#作业将在其上运行的运行器类型
运行:ubuntu最新版

#步骤表示将作为作业的一部分执行的任务序列
步骤：
#在$git ub_工作工作下签出您的仓库，以便您的工作可以访问它
-用途:动作/结帐@v4

#使用运行器外壳运行单个命令
-名称:运行一行脚本
你好，世界！

#使用运行器外壳运行一组命令
-名称:运行多行脚本
运行:|
回声添加要构建的其他操作，
回声测试，并部署您的项目。
